# PeakGlow 开发文档

> 刘海光晕负载提示工具 · macOS · MacBook Pro 14/16 (Apple Silicon)
> 版本 v1.0（待评审）

---

## 1. 项目目标

一款 macOS 菜单栏常驻小工具，实时监测 CPU 负载，通过 MacBook Pro 刘海周围的动态光晕提示当前负载状态：

| 负载档位 | 视觉效果 |
|---|---|
| 低 | 无任何提示（零渲染、零开销） |
| 中 | 刘海周围缓慢流动的彩虹光晕（类似 Apple Intelligence Siri 极光，SDR） |
| 高 | 光晕变为鲜艳蓝色，带 HDR 峰值亮度 + 呼吸脉冲动画 |

**约束**：当任何应用处于全屏（原生全屏空间或无边框窗口全屏，如游戏）时，光晕自动隐藏。

**命名**：PeakGlow（暂定）。

---

## 2. 总体架构

```
┌─────────────┐    ┌──────────────┐    ┌────────────────┐
│  CPUSampler │───▶│  LoadLevel   │───▶│  GlowController │
│  (1s 采样)  │    │  (状态机+滞回)│    │  (模式切换+过渡) │
└─────────────┘    └──────────────┘    └───────┬────────┘
                                               │
┌─────────────────────┐                        ▼
│ FullscreenDetector  │────暂停/恢复────▶ GlowWindow (刘海顶部条状透明窗口)
│ (空间通知+窗口轮询)  │                    └─ CAMetalLayer + Metal 渲染
└─────────────────────┘                        ▲
                                               │
┌─────────────────────┐                        │
│ 刘海悬停交互 + 气泡菜单│────预览/参数───────────┘
│ (NotchHover+Bubble) │
└─────────────────────┘
```

- **语言/框架**：Swift 5.10+ / AppKit（渲染层 Metal，设置面板 SwiftUI）
- **工程**：Xcode 项目（.xcodeproj），目标 macOS 14.0+
- **形态**：`LSUIElement = true`，无 Dock 图标，纯菜单栏应用
- **零依赖**：不使用任何第三方库

---

## 3. 模块设计

### 3.1 CPUSampler（CPU 采样）

- 使用 Mach API `host_processor_info(PROCESSOR_CPU_LOAD_INFO)`，对比两次采样 tick 差值，计算全部核心的总占用率（user+system / total）。
- 采样周期 1s；维护最近 5 个样本的滑动平均，避免瞬时毛刺。
- `ProcessInfo.thermalState` 作为参考信号暴露给状态栏显示（不参与分级）。
- P/E 核心加权（E 核满载但 P 核空闲时总占比偏低的问题）列为 v2 优化项。

### 3.2 LoadLevel（分级状态机）

```
enum LoadLevel: low / medium / high
```

默认参数（全部可在设置中调整）：

| 参数 | 默认值 |
|---|---|
| medium 进入阈值（5s 均值） | ≥ 60% |
| high 进入阈值 | ≥ 85% |
| 进入保持时间 | 3s |
| 降级阈值（滞回） | 阈值 −10pt |
| 降级保持时间 | 5s |

- 用滞回 + 保持时间防止档位抖动导致光晕闪烁。
- 支持设置面板"预览模式"强制指定档位（调试/展示用）。

### 3.3 FullscreenDetector（全屏检测）

两层机制：

1. **原生全屏空间（零成本）**：GlowWindow 的 `collectionBehavior` 不含 `.fullScreenAuxiliary`，系统保证在全屏空间中窗口不可见。
2. **无边框窗口全屏（轮询启发式）**：每 2s + 应用激活/空间切换通知触发，`CGWindowListCopyWindowInfo([.onScreenOnly])`：
   - 排除自身窗口、Dock、WindowServer、SystemUIServer；
   - 若某窗口 bounds 覆盖内建屏幕 ≥95% 面积（含菜单栏区域）且 layer ≤ 0 → 判定全屏 → 暂停光晕；
   - **只读 bounds/owner，不读窗口标题**，避免触发"屏幕录制"权限。
   - 可在设置中关闭此层检测（防止个别误判）。

屏幕睡眠（`NSWorkspace.screensSleepNotification`）时同样暂停。

### 3.4 NotchedScreenFinder（刘海屏定位）

- 遍历 `NSScreen.screens`，取 `auxiliaryTopLeftArea != nil` 的屏幕（macOS 12+，只有带刘海的内建屏非 nil）；
- 找不到刘海屏（外接屏使用/无刘海机型）→ 工具进入空闲模式，仅菜单栏显示 CPU%。

### 3.5 GlowWindow（光晕窗口）

- `NSPanel`，styleMask：`.borderless + .nonactivatingPanel`（不抢焦点）；
- `isOpaque = false`，`backgroundColor = .clear`，`hasShadow = false`；
- `ignoresMouseEvents = true`（完全穿透）；
- `level = statusBar + 1`（覆盖在菜单栏之上，菜单栏图标被轻微染色是预期"背光"效果）；
- `collectionBehavior = [.canJoinAllSpaces, .stationary, .ignoresCycle]`；
- `sharingType = .none` → **不出现在截图/录屏/直播画面中**；
- 窗口几何：水平居中于刘海，默认 **720pt × 96pt**（宽度可调），贴屏幕顶端；
- 屏幕参数变化（分辨率/亮度/外接）时重新定位。

### 3.6 Renderer（渲染核心，统一 Metal 管线）

单 `CAMetalLayer`，中/高两档共用一套 shader，通过 uniform 插值无缝过渡：

```
CAMetalLayer 配置（EDR/HDR）：
  pixelFormat            = .rgba16Float          // HDR 必须 float16
  wantsExtendedDynamicRangeContent = true
  extendedDynamicRangeColorSpace   = extendedLinearSRGB
  // 每 3s 轮询 NSScreen.maximumExtendedDynamicRangeColorComponentValue
  // 自适应 headroom（低电量模式/自动亮度会压低它）
```

**绘制逻辑（fragment shader）**：
- 以刘海中心为原点的径向距离场 + 2~3 阶 value noise 扰动，产生"极光流动感"而非静态径向渐变；
- **中档**：hue = 慢速时间 + 空间偏移，多色相柔混，亮度 ≤ 1.0（SDR），峰值 alpha ~0.5 保证菜单栏图标可读；
- **高档**：鲜艳蓝色（hue ≈ 220°），颜色乘以 HDR 系数 `1.0 + headroom × k × pulse`（k 默认 ~3，可调），`pulse` 为 0.5Hz 正弦呼吸，同时调制光晕扩散范围；
- 档位切换 0.8s 交叉淡变；出现/消失 0.5s 淡入淡出；
- **HDR 不可用**（headroom == 1.0，如外接 SDR 屏）→ 自动 clamp 为鲜艳 SDR 蓝。

**渲染循环**：
- `CVDisplayLink` 驱动，帧率上限 30fps（默认，可调 60）；
- **低负载时完全停止渲染并隐藏窗口**（本工具自身必须省电）；
- 仅在刘海区域光晕存在时才提交绘制命令。

### 3.7 刘海交互与气泡菜单（无菜单栏图标）

**不占用任何菜单栏空间**（无 NSStatusItem），交互全部通过刘海区域完成：

**HoverWindow（悬停检测窗口）**：
- 尺寸 = 刘海精确矩形（`auxiliaryTopLeftArea.maxX` → `auxiliaryTopRightArea.minX`，全菜单栏高度），仅覆盖摄像头死区，不遮挡两侧菜单栏图标；
- 透明 `NSPanel`（`.nonactivatingPanel`，不抢焦点），仅用于接收 `NSTrackingArea` 的 mouseEntered/Exited，视觉上完全不可见；
- 与 GlowWindow 分离（GlowWindow 保持 `ignoresMouseEvents = true` 全穿透），二者输入互不干扰；
- 同样不含 `.fullScreenAuxiliary` → 全屏空间中悬停自然失效（游戏时不会误弹）。

**触发与Dismiss逻辑**：
- 鼠标进入 HoverZone 后**驻留 0.4s**（定时器）才弹出，避免滑向两侧菜单图标时误触；
- 弹出后鼠标离开（气泡 + 刘海热区）0.8s 宽限期仍未返回 → 收起；
- 按 Esc 或点击气泡外任意处 → 立即收起。

**BubblePanel（气泡菜单）**：
- 从刘海下缘向下展开的圆角气泡（视觉类似 Dynamic Island：深色实底、连续大圆角、顶部与刘海无缝衔接）；
- 弹出/收起：0.25s 弹簧缩放+淡入淡出动画，锚点在刘海中心；
- 窗口 level 高于 GlowWindow（实底覆盖光晕，保证可读性），不抢焦点；
- **内容（精简版）**：
  - 状态行：档位徽标 + 实时 CPU%（如"高 · 91%"）；
  - 预览：低 / 中 / 高 / 自动（四个小按钮）；
  - 设置…（打开独立设置窗口）；
  - 退出。

**设置面板**（独立 SwiftUI 窗口，从气泡启动，`UserDefaults` 持久化）：

| 分组 | 设置项 |
|---|---|
| 负载 | medium/high 阈值、进入保持时间、采样周期 |
| 外观 | 光晕宽度、强度、中档峰值透明度、呼吸频率、HDR 强度系数、帧率上限 |
| 行为 | 无边框全屏检测开关、登录启动（`SMAppService`）、悬停驻留时长 |

---

## 4. 项目结构

```
PeakGlow/
├── PeakGlow.xcodeproj
├── PeakGlow/
│   ├── Info.plist               # LSUIElement, NSHighResolutionCapable
│   ├── App.swift                # NSApplication 入口
│   ├── AppDelegate.swift
│   ├── Monitor/
│   │   ├── CPUSampler.swift
│   │   └── LoadLevel.swift
│   ├── Fullscreen/
│   │   └── FullscreenDetector.swift
│   ├── Glow/
│   │   ├── GlowWindow.swift
│   │   ├── GlowMetalView.swift  # CAMetalLayer 宿主 NSView
│   │   ├── Renderer.swift
│   │   └── Shaders.metal
    │   ├── Interaction/
    │   │   ├── NotchHoverController.swift   # HoverWindow + 驻留/宽限期状态机
    │   │   └── BubblePanel.swift            # 刘海气泡菜单
    │   ├── UI/
    │   │   └── SettingsView.swift
│   └── Support/
│       ├── NotchedScreenFinder.swift
│       └── AppSettings.swift
```

---

## 5. 开发里程碑

| 阶段 | 内容 | 验证方式 |
|---|---|---|
| M1 | 骨架 + CPU 采样 + 状态机 + 菜单栏显示 | 菜单栏 CPU% 与活动监视器对比，误差 <2pt |
| M2 | GlowWindow + 中档 SDR 彩虹光晕 | 视觉验收：流动极光、菜单可读 |
| M3 | 高档 HDR 蓝 + 呼吸 + 档位过渡 | 高负载时蓝光峰值明显亮于 SDR 白 |
| M4 | 全屏检测与隐藏 | 原生全屏立即隐藏；无边框全屏 ≤2s 隐藏 |
| M5 | 气泡交互（悬停驻留/气泡菜单/退出）+ 设置面板 + 登录启动 | 滑过刘海不误弹；驻留 0.4s 弹出；全部按钮可用 |
| M6 | 打磨：低负载零渲染、屏幕变化重定位、能耗自检 | 低负载时工具自身 CPU≈0、无 GPU 提交 |

**测试负载**：`yes > /dev/null &` × N（压力分档）、全屏视频、原生全屏游戏、无边框窗口游戏。

---

## 6. 风险与边界情况

| 风险 | 应对 |
|---|---|
| "总功耗"无公开 API（powermetrics 需 root） | 本版按决策用纯 CPU%；架构预留 LoadProvider 协议，v2 可加电池放电功率（IOPS）/GPU（IOReport）信号 |
| 无边框全屏启发式误判（如全屏终端/浏览器最大化） | 默认仅"整屏覆盖且 owner 为前台 app"才隐藏 + 设置开关；最大化窗口（非整屏覆盖）不触发 |
| HDR headroom 被系统动态压低（低电量/自动亮度） | 周期轮询 headroom 自适应；不可用时回退 SDR |
| 刘海区域像素本身为黑色（摄像头区） | 光晕绘制在刘海四周，不依赖刘海内像素 |
| 菜单栏图标被染色影响可读性 | 中档峰值透明度限制 ~0.5 且可调 |
| 悬停检测窗口遮挡菜单栏图标 | 热区严格限制在刘海矩形内（由 auxiliary areas 精确计算），两侧图标不受影响；气泡打开时光晕被实底气泡覆盖，不竞争视觉 |

---

## 7. 验收标准

1. 低负载：无任何视觉/渲染活动，工具自身 CPU 占用 ≈ 0；
2. 中负载：刘海周围彩虹光晕缓慢流动，色调柔和，菜单栏可读；
3. 高负载：蓝色光晕，HDR 峰值显著、0.5Hz 呼吸，档位切换平滑无闪烁；
4. 原生全屏应用/游戏：光晕消失；无边框全屏窗口 ≤2s 内消失；
5. 光晕不抢焦点、不拦截鼠标、不出现在截图与录屏中；菜单栏零占用，鼠标悬停刘海 0.4s 弹出气泡菜单（含退出），滑过不误触；
6. 无需任何系统权限弹窗（辅助功能/屏幕录制均不需要）。

---

## 8. 暂缓项（v2 候选）

- P/E 核心加权负载；
- 电池放电功率（插电/电池双模式分级）；
- GPU 负载（IOReport）；
- 外接屏 fallback（顶部居中光晕）；
- 独立 .app 分发与签名（当前阶段 Xcode 直接 Build & Run）。

---

## 9. 开发实录（v1.0 最终实现与文档差异）

- **渲染驱动**：CVDisplayLink → **主线程 Timer（.common mode）**。CVDisplayLink 实时线程与设置锁竞争曾导致整机卡死；Timer 空闲自动停止，零开销
- **全屏检测**：无边框窗口检测（FullscreenDetector）已按用户要求**移除**，仅保留窗口 collectionBehavior 带来的原生全屏空间隐藏
- **关键 bug 修复**：
  - `NSView.convert(from: nil)` 在非翻转视图+无边框窗口下坐标错误，导致光晕画在 drawable 外（完全不可见）→ 改用 window.frame 纯算术换算
  - macOS `CAMetalLayer.drawableSize` 不随 bounds 自动更新，需在 layout() 手动同步
  - macOS EDR API 为 `layer.colorspace`（非 iOS 的 extendedDynamicRangeColorSpace）
  - SwiftUI Slider(step:) 在 macOS 26 有兼容崩溃（"max stride must be positive"）→ 用连续滑杆
- **视觉默认值调整**：光晕窗口高 120pt、glowScale=height×1.55、中档透明度 0.75、中心上移半个刘海高度

## 10. 机型检测与功率匹配（v1.1）

- **MachineProfile**：IORegistry `product-name` + `machdep.cpu.brand_string` + `hw.physicalcpu`（整数 sysctl）+ 内建屏分辨率，判定 Air / Pro14 / Pro16（含高配：≥14 物理核或 Max 芯片）
- **稳定满载参考表**：Air 22/24W · Pro14 56/67W · Pro16 61/71W（自动/高配，来自实测数据）
- **首次启动自动写入**建议值（`powerTuned` 标记，不覆盖手动调整）；设置面板显示检测摘要 + "应用建议值"按钮；"恢复默认"回退到建议值
- 负载信号 v1.1：`max(CPU%, PSTR功率/满载参考×100)`，SMC 读取为 stats 同款两步法（selector 2 + data8=9 查 keyInfo / data8=5 读字节，flt 小端解析）

## 11. 纯功率信号（v1.2，最终）

- **分级信号**：仅整机功率（SMC PSTR），CPU% 降级为纯显示；功率经 5 样本滑动平均（2.5s）+ 滞回 + 保持时间，有效滤除瞬时尖峰
- **瓦数阈值**（设置面板直接调 W，替代旧百分比+满载参考双参数）：
  - Pro 14/16：中 30W / 高 55W（用户标定）
  - Air：中 13W / 高 19W —— Air 持续满载仅 18-24W、瞬时尖峰 30-45W 转瞬即逝；高档落在持续满载区间内，瞬时高负载被均值窗口+保持时间滤除，不会误触发光
  - 滞回默认 4W
- **中档彩虹 HDR**：rainbow × (1 + (hdrBoost-1)×0.5)，约一半于蓝色的 EDR 增益，色彩不过曝

## 12. App 图标（.icon 分层格式，Xcode 26）

- **接入方式**（与参考工程一致）：`PeakGlow/AppIcon.icon/` 直接放源码树（icon.json + Assets/ 三张 1024×1024 带 alpha 图层 PNG），project.yml 设 `ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon` + `INCLUDE_ALL_APPICON_ASSETS: YES`；Xcode 构建时自动编译出 AppIcon.icns + Assets.car（含 Liquid Glass 处理与旧系统回退）
- **层级**（icon.json layers 数组第一个 = 最顶层）：notch 黑色胶囊（顶）→ glow 彩虹光晕 → background 深色玻璃底盘（底）
- **图标源码**：`icons/draw_icon_layers.swift`（CoreGraphics 矢量绘制，可改参数重导出）
- 注意：actool 对 xcassets 内嵌 .icon 不生效，.icon 必须作为独立文件被 target 引用

## 13. 收尾（v1.3 正式版）

- 光晕大小改为等比缩放（glowScale 0.4–1.6，窗口与椭圆同步）
- 气泡：移除 CPU%，主显示实时瓦数；预览档"低"→"关闭"；全部按钮 30/34pt 高 + contentShape 全区域可点
- 首次启动引导窗口（无 Dock/菜单栏图标，刘海交互提示）
- 可截图模式：正式版恒为 sharingType=.none

## 14. v1.4：手柄检测（替代全屏检测）

- GamepadDetector：GameController 框架，GCControllerDidConnect/Disconnect 通知 + 无线手柄持续发现循环（2s 间隔重启 discovery）
- 任意手柄连接 → glow.setPaused(true) 光晕暂停；全部断开 → 恢复
- 新目录 /Users/sirosafc/Documents/PeakGlow-1.4，与 1.3 互不影响
